import csv
import pymysql

# Connect to the database
connection = pymysql.connect(
    host="localhost",
    user="your_username",
    password="your_password",
    database="your_database"
)

cursor = connection.cursor()

# Open the CSV file
with open("PlantInfoData.csv", "r") as file:
    reader = csv.reader(file)
    next(reader)  # Skip the header row
    for row in reader:
        cursor.execute("""
            INSERT INTO PlantInfo (CommonName, ScientificName, `Year`, Author, Status, `Rank`, FamilyCommonName, Genus, ImageURL, Edible, SaplingDescription, PlantDescription)
            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
        """, row)

# Commit changes and close the connection
connection.commit()
cursor.close()
connection.close()
